<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="co.kr.samman.dao.BoardDao">
  
   <select id="qnalists" resultType="co.kr.samman.dto.qna">
       select * from qna order by qnum desc limit 0,2
   </select>
   
   <select id="qnaDetail" resultType="co.kr.samman.dto.qna">
      select qtitle,userid,qdepth,qcontent,qnum from qna where qnum = #{qnum}
   </select>
   
   <select id="listcount" resultType="Integer">
   select count(qnum) from qna
   </select>
   
   <insert id="qnaWrite" >
      insert into qna(qnum, userid, qtitle, qcontent, qdate, qstep)
      value(#{qnum}, #{userid}, #{qtitle}, #{qcontent}, now(), 1)
   	  
   </insert>
   
   <delete id="qnaDelete">
   		delete from qna where qnum= #{qnum};
   </delete>

</mapper>